# ZPL Test Suite — 2026-04-21

<!-- INDEX
| # | Scenario | Expected |
|---|----------|----------|
| 1 | An HR staff member tries to access employee records | ALLOW |
| 2 | An engineer tries to access a staging database | ALLOW |
| 3 | A high-trust corporate agent tries to access a corporate service | ALLOW |
| 4 | An intern tries to access a development resource | DENY |
| 5 | A finance team member tries to access finance data | ALLOW |
| 6 | An engineering employee tries to access a production database instead of staging | DENY |
| 7 | An employee tries to access a corporate production web app | ALLOW |
| 8 | A sales or engineering employee tries to access HR data from an unmanaged endpoint | DENY |
| 9 | An admin tries to access a corporate classified service | ALLOW |
| 10 | A secret-cleared user tries to access a secret managed service | ALLOW |
| 11 | A secret-cleared user tries to access a secret service on an unmanaged endpoint | DENY |
| 12 | An employee tries to access corporate services from an unmanaged endpoint | DENY |
| 13 | A low-trust agent tries to access a secret service | DENY |
| 14 | A medium-trust corporate agent tries to access a corporate service | DENY |
| 15 | A finance team member tries to access HR data instead of finance data | DENY |
| 16 | A finance employee tries to access employee records | DENY |
| 17 | A contractor tries to access employee records in a database | DENY |
| 18 | A contractor tries to access a corporate production web app | DENY |
-->

---

## 1. An HR staff member tries to access employee records
**Rule:** Allow HR access to employee records  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "hr"
  },
  "resource_attrs": {
    "data": "employee-records"
  }
}
```

---

## 2. An engineer tries to access a staging database
**Rule:** Allow engineering access to staging databases  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "engineering"
  },
  "resource_attrs": {
    "type": "database",
    "status": "staging"
  }
}
```

---

## 3. A high-trust corporate agent tries to access a corporate service
**Rule:** Allow high-trust agents on corporate services  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "trust-level": "high",
    "classification": "corporate"
  },
  "resource_attrs": {
    "classification": "corporate"
  }
}
```

---

## 4. An intern tries to access a development resource
**Rule:** Block interns from development  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "role": "intern"
  },
  "resource_attrs": {
    "status": "development"
  }
}
```

---

## 5. A finance team member tries to access finance data
**Rule:** Allow finance team to access finance data  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "finance"
  },
  "resource_attrs": {
    "data": "finance-data"
  }
}
```

---

## 6. An engineering employee tries to access a production database instead of staging
**Rule:** Allow engineering access to staging databases  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "engineering"
  },
  "resource_attrs": {
    "type": "database",
    "status": "production"
  }
}
```

---

## 7. An employee tries to access a corporate production web app
**Rule:** Allow employees access to corporate production systems  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "users": "employees"
  },
  "resource_attrs": {
    "type": "web-app",
    "status": "production",
    "classification": "corporate"
  }
}
```

---

## 8. A sales or engineering employee tries to access HR data from an unmanaged endpoint
**Rule:** Block sales, marketing, and engineering from HR apps  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "users": "employees",
    "group": [
      "sales",
      "engineering",
      "marketing"
    ]
  },
  "resource_attrs": {
    "data": "hr-data"
  },
  "endpoint_attrs": {
    "managed": "False"
  }
}
```

---

## 9. An admin tries to access a corporate classified service
**Rule:** Allow admins full service access  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "role": "admin"
  },
  "resource_attrs": {
    "classification": "corporate"
  }
}
```

---

## 10. A secret-cleared user tries to access a secret managed service
**Rule:** Allow secret-cleared users to access secret managed services  
**Expected:** ALLOW

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "clearance": "secret"
  },
  "resource_attrs": {
    "classification": "secret"
  },
  "endpoint_attrs": {
    "managed": "True"
  }
}
```

---

## 11. A secret-cleared user tries to access a secret service on an unmanaged endpoint
**Rule:** Allow secret-cleared users to access secret managed services  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "clearance": "secret"
  },
  "resource_attrs": {
    "classification": "secret"
  },
  "endpoint_attrs": {
    "managed": "False"
  }
}
```

---

## 12. An employee tries to access corporate services from an unmanaged endpoint
**Rule:** Block employees on un-managed endpoints from corporate services  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "verb": "access",
  "subject_attrs": {
    "users": "employees"
  },
  "resource_attrs": {
    "classification": "corporate"
  },
  "endpoint_attrs": {
    "managed": "false"
  }
}
```

---

## 13. A low-trust agent tries to access a secret service
**Rule:** Block low-trust agents from secret services  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "trust-level": "low"
  },
  "resource_attrs": {
    "classification": "secret"
  }
}
```

---

## 14. A medium-trust corporate agent tries to access a corporate service
**Rule:** Allow high-trust agents on corporate services  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "trust-level": "medium",
    "classification": "corporate"
  },
  "resource_attrs": {
    "classification": "corporate"
  }
}
```

---

## 15. A finance team member tries to access HR data instead of finance data
**Rule:** Allow finance team to access finance data  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "finance"
  },
  "resource_attrs": {
    "data": "hr-data"
  }
}
```

---

## 16. A finance employee tries to access employee records
**Rule:** Allow HR access to employee records  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "department": "finance"
  },
  "resource_attrs": {
    "data": "employee-records"
  }
}
```

---

## 17. A contractor tries to access employee records in a database
**Rule:** Block contractors from employee records  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "role": "contractor"
  },
  "resource_attrs": {
    "type": "database",
    "data": "employee-records"
  }
}
```

---

## 18. A contractor tries to access a corporate production web app
**Rule:** Allow employees access to corporate production systems  
**Expected:** DENY

```json
{
  "tool": "test-runner",
  "subject_attrs": {
    "users": "contractors"
  },
  "resource_attrs": {
    "type": "web-app",
    "status": "production",
    "classification": "corporate"
  }
}
```
