//
//
// tcp/3306 (MySQL)
Allow tier:web oci-workload access tier:app oci-workload
Deny tier:web oci-workload  access tier:db oci-workload


// in newcorp.network:zpr-test newcorp.tier:app endpoints to connect to newcorp.tier:database endpoints

// in newcorp.network:zpr-test VCN allow newcorp.tier:proxy endpoints to connect to newcorp.tier:employee endpoints with protocol='tcp/3001'